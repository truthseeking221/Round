{
  "name": "moneycircle",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tact --config tact.config.json",
    "test": "npm run build && vitest run",
    "test:watch": "npm run build && vitest",
    "format": "node -e \"console.log('no formatter configured')\""
  },
  "dependencies": {
    "@tact-lang/compiler": "^1.6.13",
    "@ton/core": "^0.60.0",
    "@ton/crypto": "^3.3.0",
    "@ton/sandbox": "^0.30.0"
  },
  "devDependencies": {
    "@ton/ton": "^15.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
